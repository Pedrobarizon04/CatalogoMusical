<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Editar Disco</title>
  <link rel="stylesheet" href="/css/editDisc.css">
</head>
<body>
  <div class="app-container">
    <!-- Sidebar -->
    <header class="navbar">
      <div class="logo-container">
        <img src="/images/Logo.png" alt="Logo" class="logo">
      </div>
      <div class="navbar-title">
        <h1>Catálogo Musical</h1>
      </div>
      <ul class="nav-links">
        <li><a href="/" class="link">Início</a></li>
        <li><a href="/discs" class="link active">Discos</a></li>
        <li><a href="/artists" class="link">Artistas</a></li>
        <li><a href="/genres" class="link">Gêneros</a></li>
      </ul>
    </header>

    <!-- Main Content -->
    <main class="main-content">
      <h1 class="page-title">Editar Disco</h1>

      <form action="/discs/edit/<%= disco.id %>" method="POST" class="edit-form">
        <label for="titulo">Título:</label>
        <input type="text" id="titulo" name="titulo" value="<%= disco.titulo %>" required>

        <label for="ano">Ano de Lançamento:</label>
        <input type="number" id="ano" name="ano" value="<%= disco.ano %>" required>

        <label for="capa">URL da Capa:</label>
        <input type="url" id="capa" name="capa" value="<%= disco.capa %>" required>

        <label for="faixas">Faixas (separadas por vírgula):</label>
        <textarea id="faixas" name="faixas" rows="5" required><%= disco.faixas.join(', ') %></textarea>

        <label for="genero">Gênero Musical:</label>
        <select id="genero" name="genero" required>
          <% generos.forEach(genero => { %>
            <option value="<%= genero.id %>" <%= disco.GeneroId === genero.id ? 'selected' : '' %>><%= genero.nome %></option>
          <% }) %>
        </select>

        <button type="submit" class="btn-primary">Salvar Alterações</button>
      </form>

      <!-- Centralizar botão voltar -->
      <div class="centered-link">
        <a href="/discs" class="btn-secondary">Voltar para a lista de discos</a>
      </div>
    </main>
  </div>
</body>
</html>
